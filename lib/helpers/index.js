const Identifier = require('./identifier');
const UUID = require('./uuid');
const Setuper = require('./setuper');
const RequireIfExists = require('./require-exists');

module.exports = {
  Identifier,
  UUID,
  Setuper,
  RequireIfExists
};
